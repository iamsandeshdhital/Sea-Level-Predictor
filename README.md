# Sea Level Predictor

This is the freeCodeCamp Sea Level Predictor project.

## Run

Install the dependencies and generate the chart:

```powershell
py -m pip install -r requirements.txt
py main.py
```

The result is saved as `sea_level_plot.png`.
